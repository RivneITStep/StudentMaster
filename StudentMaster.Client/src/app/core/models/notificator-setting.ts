export class NotificatorSetting {
  msg: string;
  bgColor: string;
  fgColor: string;
  constructor(msg: string, bgColor: string, fgColor: string) {
    this.msg = msg;
    this.bgColor = bgColor;
    this.fgColor = fgColor;
  }
}
